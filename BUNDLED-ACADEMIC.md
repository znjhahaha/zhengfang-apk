# 金智、乘方通用协议与预置学校

金智与乘方提供可供多校复用的通用基础协议，分别通过 `builtin.jinzhi` 和 `builtin.chengfang` 供 API 3 配置或扩展插件继承。新学校使用本校教务地址、认证配置和网络权限，基础查询可直接复用，学校特有的接口与登录流程可由扩展插件补充。

通用协议需要结合学校配置使用。同厂商学校的接口版本、认证入口和作息仍须验证，选择一个教务类型并不代表任意学校已经完成适配。

## 开箱可用的预置学校

App 随安装包提供湖北汽车工业学院金智教务和山东石油化工学院乘方教务的预置适配，无需从插件站导入。两者出现在教务类型列表，并可通过学校教务地址或指定统一认证入口识别；统一认证地址会归一化到实际教务后端。学校识别以这些预置地址为依据，通用金智 CAS 登录页本身不能确定教务系统类型。

内置程序与独立 `.eduplugin` 来自同一份源码。插件仓库运行 `node scripts/sync-bundled-academic.mjs <App仓库>`，先检查类型、契约和样本，再产生 `app/src/main/assets/bundled-academic/`。添加 `--check` 验证源码与 App 内置内容一致。

预置包由 APK 信任边界保护，同时校验固定 ID、文件名、学校、类型、ZIP 摘要及包内源码摘要。加载器只读固定 App 资源，不接收外部路径，不改变外部包必须验证签名的规则。已安装的官方适配优先；手动选择保留优先级。这两份预置学校配置只匹配对应学校与端口，通用协议的跨校复用通过下述继承配置完成。

登录、网页登录 Cookie 导入、会话恢复、课表、成绩、考试、作息及选课查询均走现有 App 网关。插件状态依旧隔离于学校和账号。

- 湖北汽车工业学院：统一认证、课表、成绩、考试、作息、课程、教学班、已选和学校开放时的选退协议；真实验收仅查询。
- 山东石油化工学院：官方统一认证网页登录后查询身份、学期、课表、成绩、考试、作息、选课类别、教学班与已选。统一认证密码不用于独立教务直登。当前官网非选课时间；提交协议未验证，不能声称选退提交完成。未开放的成绩分项也不作空结果。

乘方网页查询要求 Referer。API 3 教务 HTTP 的可选 `sameOriginReferer` 由宿主生成当前目标站点的根地址，每次重定向重新生成；不会带入上一个站点或路径、查询参数、票据。普通插件不能自行填写该请求头。

## 跨校继承与 SDK 3.1.0

SDK 3.1.0 保持 API 版本为 3，提供 `extend-jinzhi`、`extend-chengfang` 模板，包含虚构学校的配置与合成测试。

后续学校必须明确配置本校 `school`、`network` 和 `builtinConfig`：金智提供 CAS 根地址和本校作息，乘方提供完整官方 SSO 入口。未覆盖的方法走内置协议，并使用当前插件声明的网络权限和学校地址；不会默认为湖北汽车工业学院或山东石油化工学院。自定义认证和基础查询可在同一学校、账号、提供者的状态空间交接会话，跨学校、账号保持隔离。

认证和选课能力组须整体覆盖；课表、成绩、考试、作息可逐项覆盖。同厂商的新版本仍须验证接口和数据结构。乘方的基础选退提交仍为未支持，继承不能绕过此限制。外部派生插件仍需要正常签名或明确的开发导入，不继承内置包的信任标记。
