# 教务插件 v1 本地验收

基线：`17ada56634b4ce89b88c3e8e3978c2ae0a961692`。工作目录：`D:\zfapk-plugins-v1`，分支：`codex/academic-plugins-v1`。独立玻璃性能改动保留在原工作区。

## 底栏

- 动画周期保持 480ms。切换时保留正在播放的轨迹与速度，清除待播放反馈；连续点击合并为一次待播放反馈。
- 五个图标的几何与填充在首末帧一致、末速度归零。柱形图改变顶边位置，保持描边宽度。文字使用固定布局的常规／加粗双层渐变。
- `NavigationIconMotionTest`：2 项通过，覆盖端点导数、运动范围及柱形图速度连续性。
- API 32 `NavigationIconPlaybackDeviceTest`：2 项通过，覆盖中断、50 次重复点击、减少动态效果、静止状态，以及浅深色五个图标 90%～100% 的末段帧。
- 末段图片位于本地 `build/navigation-frames/`；首末帧逐像素相同。已查看联系图，未见静止前后的轮廓跳动或闪色。
- `assembleUiPreview`、`assembleUiPreviewAndroidTest` 与专项 JVM 测试成功，101 个任务执行。

## 基线回归依据

迁移前已验收的基线为 474 项 JVM 测试，其中 470 项通过、4 项有条件在线测试跳过。新的全量验证结果在各阶段完成后记录。

## 在线限制

南京工程学院账号在线验收仍受此前凭据拒绝限制；本次不重试该账号。选退课验证使用虚构学校或本地模拟服务。
