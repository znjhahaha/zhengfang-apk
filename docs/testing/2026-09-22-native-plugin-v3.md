# 原生扩展 API v3 验收

2026-09-22，Windows / JDK 21 / MuMu Android 15，开发目录 `D:\zhengfang-vps-resume\app`。

## 实现

支持 API v1/v2/v3；增加五种原生块、两种表单字段、三个入口位置和四种逐次确认的系统操作。服务会话继续与教务会话隔离。文件只返回本次选择内容，64 KiB 上限；扫码不自动导航；日历只声明编辑器 opened，不声称保存。

入口只显示已启用、匹配学校的正式包。用户按账号、插件、入口和页面位置保存显示设置。未签名包从开发预览打开。

## 验证

- 全量重新执行 `testUiPreviewUnitTest / assembleUiPreview / assembleUiPreviewAndroidTest / lintUiPreview`，使用 `--no-build-cache --rerun-tasks`，88 个任务执行，构建成功。507 项 JVM 测试：503 通过、4 项需要真实账号的测试跳过，无失败。新增契约测试覆盖 API 降级、非法回调、真实日历日期、文件大小/编码/类型、失败结果无数据、表格结构和学校入口过滤。
- `lintUiPreview` 通过，保留既有警告，不声称警告清零。
- 旧插件的 16 项模拟器回归通过，覆盖校园服务、管理中心、签名目录、沙箱、安装和更新。
- 新增 4 项设备测试通过：原生页面/多页导航/表单与全部授权拒绝；真实系统文件选择和通知来源；账号/会话引用/epoch/插件启用状态撤销；入口设置隔离。
- 新 UI 截图通过 Compose 页面根节点或授权对话框节点采集，确保捕获受测窗口。MuMu 的默认显示器可能返回桌面，不能用它作为页面验收证据。系统通知权限由外部运行脚本授权和恢复，避免 Android 15 在撤销权限时终止 instrumentation。
- 截图检查发现滚动内容压到固定标题，修正服务页滚动视口的顶部留白并增加网格按钮内边距；重新构建及复核新 UI 与旧服务 UI。
- 课表适配与滚动状态的 4 项模拟器回归通过，覆盖不同屏幕宽度与字体比例、长教室名称、切周滚动位置和布局恢复。预览 APK 已重新安装并冷启动成功。
- 节点 SDK/CLI 测试 25 项：21 通过，4 项 Windows 文件符号链接条件跳过。目录 junction 拒绝测试通过。
- 网站 10 项 Worker 测试、TypeScript 检查和构建通过。47 文件 starter ZIP 独立解压后 npm ci/test，以及六模板 create/check/test/pack/source-zip 全部通过。
- 网站部署版本 `89dcb8c3-7a5a-46a0-a79d-e903fe1a1c05`；线上文档和招募群核对通过，开发工具包 SHA-256 与本地构建一致。

## 已有学校与限制

本次没有修改太原科技大学、浙江工业大学、湖南工业大学或正方 CAS 的已有 SSO 实现，也没有修改三亚学院在 821da95 中修复的登录判断。SSO 单元测试均通过。`PasswordLoginGatewayFactory` 仅在插件显式覆盖 auth.start 时优先插件；配置与局部学习数据扩展继续复用本体认证。

本轮未重新连接真实学校账号，也未做真实选退课。相机实际解码、日历保存和 API 24 实机未验证；不能以契约和模板测试代替这些验收。华南师范大学成绩明细仍为样本模板，黑龙江工程学院网络与南京工程学院真实查询验收限制保持。

## 问卷与交付授权

用户选择保留 12 题并增加功能评分和使用场景。问卷星 `386005917 / YRS3nyN` 已发布；题目、选项、选填设置和评分矩阵读回一致。通过设置接口单独补齐结束页招募文案，核对 QQ 群 1074017033。旧初稿 385983663 保持暂停。

App 问卷投放 ID `10315886-bea1-43d1-a42e-7e4845f229d1`，published / active，audience=all，important=true。公开入口为 https://v.wjx.cn/vm/YRS3nyN.aspx。未提交模拟答卷。矩阵的“未使用/无法评价”是分类选项，后续统计评分时须排除该选项。

用户随后明确要求推送、合并到 main 并执行 CI/CD，覆盖原先本轮不发版的限制。版本 1.0.81 更新日志包含 v1.0.80 之后全部功能提交。CI/CD 的最终结果以 GitHub 实际运行和发布资产核对为准。
